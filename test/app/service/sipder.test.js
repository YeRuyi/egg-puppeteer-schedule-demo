const { app } = require('egg-mock/bootstrap');

describe('spider service test', () => {
  // it('should load cities info', async () => {
  //   const ctx = app.mockContext();
  //   await ctx.service.spider.loadCitiesInfo();
  // });


  // it('should load articles', async () => {
  //   const ctx = app.mockContext();
  //   await ctx.service.spider.loadAttractionsInfo();
  // });
});
